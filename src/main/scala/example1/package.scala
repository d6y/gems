package object example1 {
  // Hypothetical: imagine this reading some id from a file and returning an optional value.
  // I.e., faked implementation so we compile.
  def read(id: String): Option[String] = None
}